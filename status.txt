CURRENT CHECKPOINT — 2026-09-23 PDT — MATRIX PUBLISHED; ANCHORING ANALYSIS DONE; GENERALIZATION RESEARCH RUNNING

WHERE THIS IS
  Repo: /nfs/hpc/share/sanchej7/isaac-sim-pruning-workflow (reachable as ~/hpc-share/...).
  Branch develop. Local history now CONTAINS origin/develop 8edb3d7 (merged clean).
  Pushed through 9831fc1. Ask before any further push; never force-push.
  CPU environment: /nfs/hpc/share/sanchej7/miniforge3/envs/depth-env/bin/python
  (Python 3.10.19, numpy 2.2.6, opencv 4.11.0.86, ruff 0.16.5). No repo venv exists.

SIX-PHASE PLAN IN PROGRESS (user prompt of 2026-09-22)
  Phase 0 audit ...... DONE (plan delivered in chat)
  Phase 1 lighting ... DONE
  Phase 2 success rate  DONE. 0/40 (Wilson 95% 0-0.088). 20 of 40 trials were
                       infrastructure: a target-register error (unlisted tree1
                       spurs). Only tree0 was measured. See PHASE 2 RESULT.
  Phase 3 ROS 2 SIL .. DONE. 199/199 recorded decision states reproduced.
                       Evidence docs/evidence/ros2_sil_parity_2026-09-23.json.
  Phase 4 studio ..... DONE. Static replay page at studio/replay, 1.3 MB payload,
                       browser-tested at 390px and 1280px. Pages workflow added;
                       ONE REPO SETTING STILL NEEDED, see PHASE 4 below.
  Phase 5 CuRobo ..... STOPPED AT TIME BOX. Not in the pinned stack; running it
                       needs a CUDA-extension build and a GPU allocation. Nothing
                       installed. docs/evidence/curobo_feasibility_2026-09-23.json
  Phase 6 docs/claims  DONE. Five claims (PRU-SIL, PRU-CPP, PRU-LIGHT, PRU-RATE,
                       PRU-STUDIO) appended ABOVE the generated marker in
                       /nfs/hpc/share/sanchej7/Jose_Sanchez_Portfolio.com/career/CLAIM_BANK.md
                       (additive only; pre-edit copy at
                       /scratch/sanchej7/tmp/CLAIM_BANK.before-2026-09-23.md).
                       Their SHA links resolve only after this repo is pushed.
                       Nothing committed in the portfolio repo. Resume bullets and
                       outreach openings were delivered in chat, not written to files.
  DONE 2026-09-23: develop pushed; Foundation, ROS 2 SIL and Pages all green.
    ROS 2 SIL first failed on `set -u` while sourcing ROS; fixed in 9831fc1.
    The first Pages deploy after enabling Pages hung in "waiting" with no gate;
    cancelled and re-dispatched, it deployed. Replay studio verified live at
    https://joses2017smjh.github.io/isaac-sim-pruning-workflow/ (headless
    Chromium, 390px and 1280px, no errors). CLAIM_BANK entries marked verified.
ENVY/UFO + LIGHTING + LEARNED DEPTH (2026-09-23, after the user asked)
  What this thread is: the LEARNED metric-depth models (DA2, DINO) from the
    depth project, NOT the controller's LK tracker. Two questions: carry-over
    Envy -> UFO, and across lighting presets. Offline on rendered frames.
  DONE: Sept 20 tooling committed byte-for-byte as it ran (3f3559f). Results
    that were never written up are now regenerable and published:
    docs/evidence/stage_a_depth_2026-09-23.json  (DA2 on Isaac RTX: 0/234 within
      20 mm, +0.4-0.6 m overestimate, latency 0.25 s; every gate fails)
    docs/evidence/family_pilot_depth_2026-09-23.json (evening 6.1x/7.9x worse,
      N=1 tree per family; DINO adds nothing; DA2 p95 0.29 s, DINO 2.2 s)
    Regenerate: PYTHONPATH=/nfs/hpc/share/sanchej7/sil-pydeps python \
      tools/aggregate_family_eval.py --evaluation da2=<eval.json> [--evaluation dino=...] --output <json>
  FIXED: target metric scored background (3x3 unmasked window on a 1-px spur).
    depth_generalization.target_metrics now takes mask=; rows carry
    target_masked beside the unchanged target. 21370047 was still "RUNNING" in
    docs; corrected to COMPLETED, gates FAILED.
  REGISTERED, NOT SUBMITTED: docs/EVAL_PROTOCOL_FAMILY_LIGHTING_2026-09-23.md.
    8 trees (Envy 00000/03/08/12 = DA2 VALIDATION trees, not unseen; UFO 00000-03,
    absent from fine-tuning), 4 Blender lights x 6 views, DA2 + DINO, mask-gated
    target metric, Sept 20 gates unchanged (expected to fail). Pilot trees
    reused; 6 to render. 120 GPU-min reserved, ~11 actual, ~0.4 GB, one GPU.
  DONE 2026-09-23: render array 21402687 (0-5%1) and eval 21402688 both
    COMPLETED; all 8 trees scored; ~8 GPU-min used of 120 reserved.
    RESULT (docs/evidence/family_matrix_depth_2026-09-23.json):
      lighting: evening 6.1-8.4x source in ALL 8 trees (reading 1 replicated)
      family:   UFO worse than Envy under every light, no overlap (+5-9 cm
                daylight, +54 cm evening) BUT debiased UFO <= Envy: it is a
                larger constant under-estimate, not worse shape; Envy trees
                are validation trees so exposure is a confound
      gates:    all fail as registered; masked target 4/116 within 20 mm
      DINO:     adds nothing, 2.2 s six-view latency
      caveat:   spur target invisible in every view for 3 of 8 trees
                (envy_00003, ufo_00001, ufo_00003); target-level N = 5 trees
      clips:    8 MP4s composed (CPU) into
                artifacts/generalization/family-matrix-20260923/clips/ (30 MB,
                LOCAL ONLY, not redistributed). eight_clip_manifest_2026-09-20
                marked executed. Pooled metrics agree with the SQL aggregation.
    Regenerated with (kept for the record):
      B=artifacts/generalization/family-matrix-20260923
      cat $B/eval/render_status.json          # which of the 8 trees actually rendered
      PYTHONPATH=/nfs/hpc/share/sanchej7/sil-pydeps python tools/aggregate_family_eval.py \
        --evaluation da2=$B/eval/da2/evaluation.json --evaluation dino=$B/eval/dino/evaluation.json \
        --output docs/evidence/family_matrix_depth_<date>.json
    Judged against the registered readings in EVAL_PROTOCOL_FAMILY_LIGHTING
    (result section appended there); README / ROADMAP / SLURM_JOBS updated;
    clips composed. All committed in 5371cd0. NOTHING PUSHED: six commits
    ahead of origin/develop await the user's word.
  ANCHORING (2026-09-23, zero GPU, pre-registered, P1 REFUTED), commit 65e1b23:
    docs/EVAL_PROTOCOL_DEPTH_ANCHORING_2026-09-23.md + evidence
    docs/evidence/depth_anchoring_2026-09-23.json, tool tools/anchor_depth_analysis.py,
    SQL sql/anchoring/. One metric range at the target: UFO daylight 0.19->0.06 m
    (works); Envy 0.13->0.18 m at source (WORSE: spur error != frame error,
    ceiling 0.05 says many-point fit would work); evening ceiling 0.10-0.13 and
    Isaac ceiling 0.37-0.46: NOT scale problems, need model-input changes.
  IN FLIGHT: research workflow wf_89122771-6b8 (training pipeline/data, camera
    FOV, ToF anchoring path, literature -> ranked plan -> critic). Next: register
    GPU controls (Isaac-FOV/close-range renders, original-tree renderer control,
    overcast, evening test-time normalization), state budget, ask ONCE, submit.
    User pre-authorized "queue the experiments"; the budget still gets stated.
  NOT DONE, STATED IN THE PROTOCOL: renderer/distance controls for Stage A;
    'overcast' preset (darkness vs sun angle); controller on Envy/UFO (no path:
    RenderEnv presents the Blender orchard; L-Py USDs have no spur-selection
    route). These need their own registration.

  OPTIONAL, USER'S CALL: commit CLAIM_BANK.md in the portfolio repo (beside the
    user's own uncommitted resume edits); decide the fate of studio/src; add
    repo topics; GPU OK only if a tree1 re-registration is wanted.
  Agreed gating: run CPU-only work continuously; hard-stop for GPU budget
  approval, any push, and committing further untracked work.

PHASE 1 RESULT — lighting pilot 21360571 published
  All six pre-registered tasks accounted for; zero incomplete.
  Grades RECOMPUTED by the independent grader; all six agree with stored grades.
  6/6 pass 17/17 checks. Release 7.8 s (7.7 s for task 5), 68 commands (67),
  809.49 mm fall, home error 0.000248-0.001140 mm.
  First tracking loss is always the frame AFTER release (detached piece leaving
  the ROI during retreat), reason optical_flow_failed. No run recorded a stop.
  CLAHE DECISION: DROPPED, recorded as a rejected diagnostic. It changed no task
  outcome and no tracking continuity, so it fails the pre-registered primary
  comparison. The option remains in the config for reproducibility only.
  MARGIN FINDING (secondary, not the primary metric): morning raw fell to
  4 surviving features against the tracker's own min_features floor of 4, and
  confidence 0.1735 against min_confidence 0.15. It completed with no margin.
  CLAHE raised the feature floor in all three presets (18->21, 4->7, 13->15) and
  the confidence floor under source and morning, but LOWERED it under evening
  (0.500->0.403). Not a uniform improvement.
  => morning light is the discriminating condition and belongs in Phase 2.

PHASE 1 ARTIFACTS
  tools/summarize_lighting_pilot.py   the one documented regeneration command
  tests/test_summarize_lighting_pilot.py   7 tests
  docs/evidence/lighting_pilot_results_2026-09-23.json   regenerated evidence
  SLURM_JOBS.md   enriched 8-row table incl. loss reason, loss time, drop, margins
  README.md "Result"   short pilot summary and the drop decision
  docs/RESEARCH_EXPERIMENTS_2026-09-19.md   "Decision on the CLAHE candidate"
  docs/ROADMAP.md   new checked gate with the evidence link
  Regenerate with:
    python tools/summarize_lighting_pilot.py \
      --batch-dir artifacts/vision_robustness/lighting-20260919 \
      --output docs/evidence/lighting_pilot_results_2026-09-23.json

VALIDATION AT THIS CHECKPOINT
  pytest -q -m "not isaacsim_ci" (tracked scope): 539 passed, 9 skipped,
    1 deselected. The 9 skips all need pxr (USD), absent from the CPU env.
  ruff check + ruff format --check on source/tests/tools: clean, 134 files.
  tools/run_pruning_demo.py: reproduces 42 / 20 / 35 frames, exit 0.
  KNOWN FAILING, UNTRACKED, DEFERRED TO PHASE 4:
    tests/test_studio_orchard.py::test_actual_local_glb_matches_manifest_units_
    original_tree_separation_and_hashes asserts tree separation > 0.1, actual 0.0.
    Real bug in the untracked glTF exporter: tree1 lands on the origin.

PHASE 2 RESULT (2026-09-23) — evidence docs/evidence/eval_2026-09-23.json
  Aggregated through sql/*.sql by tools/aggregate_eval.py (DuckDB).
  Inclusive headline 0/40, Wilson 0.000-0.088. Per light 0/20 each (0-0.161).
  Excluding infrastructure 0/20 (0-0.161); also excluding layout 0/14 (0-0.215).
  Taxonomy: infrastructure 20, layout startup contact 6, hazard_contact 6,
    vision_invalid 6, tof_minimum_clearance 2. Every target ended identically
    under source and morning light: geometry decided, lighting did not.
  MY ERROR, DISCLOSED IN THE PROTOCOL: all 10 tree1 targets were outside the
    export's listed candidates, which the renderer refuses ("Unlisted tree1
    components require a new geometry audit"). Recorded in Phase 0, not
    re-applied when sampling widened to 444 spurs. ~1 GPU-minute per task wasted.
    Kept in the denominator. queue_vision_robustness.unpresentable_targets now
    refuses such a register on CPU; the registered register was NOT rewritten.
  Open gate: evaluate tree1 with a NEW registration (listed candidates only, 7
    pass jaw fit, or after the geometry audit). Needs the user's GPU OK.
  Still: docs/demo/eval_failure_hazard_contact.png (tied most common stop).

PHASE 2 — SUBMISSION RECORD (2026-09-23)
  Protocol: docs/EVAL_PROTOCOL_2026-09-23.md (committed BEFORE any submission).
  Target register: docs/evidence/eval_targets_2026-09-23.json
    Population measured from the hash-verified export: tree0 310 components,
    tree1 399 — matching the exporter's own recorded component_count.
    Jaw-fit screen 0 < max_radius_m <= 0.012 admits 185 + 259 = 444 spurs.
    Sampled 10 per tree, seed '20260923:tree<N>', N = 20. Radius 4.85-10.07 mm,
    length 47.9-100.6 mm. The n=1 success target 8235 is NOT in the set.
    265 screened-out components are listed by vertex ID for audit.
  Regenerate (needs USD, so the pinned Isaac venv):
    /nfs/hpc/share/sanchej7/Humanoid_Lite/venv-isaac60/bin/python \
      tools/enumerate_spur_targets.py \
      --export-dir artifacts/blender_scene/orchard_two_trees_v1 \
      --per-tree 10 --seed 20260923 \
      --output docs/evidence/eval_targets_2026-09-23.json
  Launcher now supports target sweeps and sizes the Slurm array from the frozen
    plan (--array=0-N-1%1) instead of the sbatch directive, so a sweep cannot
    launch more or fewer tasks than it registered. Duplicate targets are refused.
    Omitting --targets-file reproduces the original six-run lighting pilot.
  SUBMITTED (user approved 660-680 GPU-min for both arrays):
    21400715 targets-source-20260923   source light, 0-19%1, 340 GPU-min
    21400716 targets-morning-20260923  morning light, 0-19%1, 340 GPU-min,
             --dependency=afterany:21400715 so only one GPU runs at a time.
    Frozen code revision 00818fb034dd3a6fec721825cd2e353f1b545a0d.
    Both PENDING at submission: A on (QOSMaxGRESPerUser) behind the user's
    unrelated lh-cl2-bench array, B on (Dependency). NOTHING WAS MODIFIED.
    SUBMISSION IS NOT EVIDENCE. No capture, no grade, no rate exists yet.
  WHEN THE ARRAYS FINISH, resume here:
    Grade with tools/summarize_lighting_pilot.py per batch (it is condition
    agnostic: it reads plan.json and reports any planned run with no capture as
    incomplete), then build the DuckDB layer described below.
  STILL TO BUILD AFTER THE RUNS LAND: the DuckDB aggregation layer in sql/,
    Wilson 95% interval, failure taxonomy from recorded stop reasons, tracking
    coverage vs error, docs/evidence/eval_<date>.json, README Result update,
    and one GIF or still of the most common failure mode.

PHASE 3 RESULT — ROS 2 Humble software-in-the-loop (NOT hardware-in-the-loop)
  ros2/pruning_sil (ament_python) + ros2/pruning_sil_cpp (ament_cmake, gtest).
  The node imports VisionPruningDemo; it reimplements no perception or control.
  PARITY on capture 21328323: 199 of 200 frames compared (frame 0 records
    controller_source_frame_index=-1, so the node publishes an explicit hold with
    no pose). DECISION STATES AGREE 199/199. Command delta error median 0.07 mm,
    p95 0.68 mm, max 1.995 mm against a stated 2 mm tolerance.
  Divergence is in the tracker's accumulated feature state, not the control path:
    measurement error median 2.7 mm is LARGER than command error median 0.07 mm,
    because the bounded step attenuates it. Rebuilding with OpenCV 4.11.0 (the
    recording's version) gave IDENTICAL numbers, so the OpenCV version is not
    the cause. That hypothesis was tested and rejected.
  NEGATIVE CONTROLS: RGB blackout and depth dropout each hold 39/39 frames and
    never authorize approach or release.
  CONTAINER: /nfs/hpc/share/sanchej7/containers/pruning_sil_humble.sif, built by
    ros2/containers/pruning_sil_humble.def from docker://ros:humble-ros-base plus
    rosbag2-storage-mcap, python3-opencv, CPU torch and packaging>=23.2.
    APPTAINER_CACHEDIR/APPTAINER_TMPDIR must point at the share; home is 92% full.
    The packaging pin is required: the torch install pulls a setuptools whose
    metadata path breaks colcon against Ubuntu 22.04's packaging 21.3.
  RUN: apptainer exec --bind /nfs/hpc/share/sanchej7 <sif> bash -lc \
    'source /opt/ros/humble/setup.bash && cd <repo> && export PYTHONNOUSERSITE=1 \
     PYTHONPATH=$PWD/source/isaaclab_pruning:$PWD/ros2/pruning_sil:$PYTHONPATH && ...'
    PYTHONNOUSERSITE=1 matters: ~/.local has a pytest that breaks in-container.
    Do NOT replace PYTHONPATH wholesale; append, or rosbag2_py disappears.
  TESTS: 18 python tests in ros2/pruning_sil/test (procedural fixture, no
    licensed assets) and 5 gtest cases in pruning_sil_cpp. colcon build and
    colcon test are green for both packages.
  CI: .github/workflows/ros2.yml on ros:humble-ros-base (NOT ros:humble, which
    has no rosbag2), with a step that fails if any 3D asset reaches the build.
  ROADMAP: the stretch gate is split into SIL done (with evidence) and HIL
    pending hardware.

PHASE 4 RESULT — replay studio
  studio/replay is a STATIC page: no bundler, no node_modules, no build step.
  Publishes three recorded runs: 21328323 success (17/17), 21317409 closure
  failure (9/17, graded here since the capture stored no grade), and
  targets-source-20260923 run_00 tree0 v530, a real sweep failure that stopped on
  hazard contact (8/17). Shows synced wrist/overview video, phase strip, tracker
  confidence and feature count against their reject floors, both 8x8 ToF grids
  with invalid zones hatched, gate state and reason, and proposed vs applied
  commands. Every value is read from a capture; unrecorded fields say
  "not recorded" rather than being filled in.
  Payload 1.3 MB against a 12 MB budget. Verified in headless Chromium at 390px
  and 1280px: no console errors, no horizontal overflow, values change on scrub.
  Regenerate the data with:
    python tools/export_studio_replay.py --output studio/replay/data \
      --run "success=artifacts/isaac_render/job_21328323=Full vision-guided sequence=pass" \
      --run "closure_failure=artifacts/isaac_render/job_21317409=Closure failure, tracking confidence lost=fail" \
      --run "sweep_hazard_stop=artifacts/vision_robustness/targets-source-20260923/run_00_source_tree0_v530=Success-rate sweep, stopped on hazard contact=fail"
  *** ONE REPO SETTING NEEDED: GitHub > Settings > Pages > Build and deployment >
      Source = "GitHub Actions". Then .github/workflows/pages.yml publishes
      studio/replay on each push to develop. URL will be
      https://joses2017smjh.github.io/isaac-sim-pruning-workflow/ ***
  The OLDER untracked prototype in studio/src is NOT published and NOT committed.
    It is a three.js scene viewer that animates a synthetic 12 s joint loop, not
    a recording, and tools/export_studio_assets.py copies mock-pruner CAD meshes
    (its own docstring says the output must stay out of public builds). Its
    untracked test tests/test_studio_orchard.py still fails on a real exporter
    bug: the glTF rebase puts tree1 at the origin, so the two trees collapse.
    ASK THE USER what to do with it; nothing of theirs was modified or deleted.

KEY FACTS FOR PHASE 2 (established in the Phase 0 audit)
  tools/queue_vision_robustness.py freezes with `git archive HEAD`, so it REFUSES
    to submit while any tracked file is dirty. Commit before submitting.
  Targets are already switchable: PRUNING_COMPONENT_VERTEX and PRUNING_TARGET_TREE.
    blender_demo_scene.spawn_blender_demo_scene now takes target_tree_index.
  Candidate pool: docs/evidence/blender_two_tree_export.json,
    branch_geometry_candidates[tree0_SPUR] 310 components / 310 eligible / 10 listed,
    [tree1_SPUR] 399 / 398 / 10 listed. Listing rule sorts by (-|center_x|,
    first_vertex) then truncates to 10. Unlisted tree1 components are REFUSED.
  The current n=1 target 8235 is NOT in either listed set, so Phase 2 is
    independent of the existing success.
  The scene TRANSLATES so the selected spur centroid lands at a fixed workspace
    pose (0.251, 0.634, 0.815), checked to 1e-5 m. So trials vary spur radius,
    length, AXIS ORIENTATION and local occlusion at a canonicalized approach
    pose. Reachability is moot by construction; say so in the protocol.
  Offline jaw-fit screen needs no pxr: select_component requires
    0 < max_radius_m <= 0.012 and runs on the manifest dict.
  Visibility is checked INSIDE the GPU job, so a rejected target still costs
    allocation minutes. Count rejections in the denominator.
  Budget: mean 16.5 min/task on one A40 at %1; ONE RUN DIRECTORY IS 398 MB.
    Proposed array A = 20 targets x source (330 GPU-min, ~5.5 h, ~8.0 GB);
    array B = the same 20 x morning (same again). Separate OKs.
  Share /nfs/hpc/share has 162 GB free. Home is 92% full: keep outputs off home.
  duckdb is not in pyproject and there is no sql/ dir; both are new in Phase 2.

HISTORICAL AUDIT SNAPSHOT FOLLOWS

isaac-sim-pruning-workflow — audited checkpoint, 2026-09-20 PDT

SCOPE AND REPOSITORY
  Stay in isaac-sim-pruning-workflow; companion data/code read-only for this audit.
  Branch develop; inspected commit 78c01c466d598c4ddd9dadc3b6e4f759ca180847.
  Local Computer_Vision companion has no usable Git metadata/root README;
  its branch, commit and remote identity remain unverified.
  Existing untracked studio/exporter/tests/daylight-media work is preserved.
  No new job submitted, cancelled, held/released or otherwise modified.

JOBS AND RESULTS
  Queue checked 2026-09-20 17:00 PDT: no active prune-* jobs.
  Array 21360571_[0-5] COMPLETED (0:0), all six independently pass 17/17 checks.
  Frozen experiment code b4b4250fae14df0c1f39c70f60cf21960bae90ba.
  Task  Light    Method  Raw job ID  Release  Commands  Home error (mm)
  0     source   raw     21362298    7.8 s    68        0.001140
  1     source   CLAHE   21363248    7.8 s    68        0.000248
  2     morning  raw     21363331    7.8 s    68        0.000426
  3     morning  CLAHE   21363374    7.8 s    68        0.001094
  4     evening  raw     21363432    7.8 s    68        0.000484
  5     evening  CLAHE   21360571    7.7 s    67        0.001101
  Earlier full runs 21358986 (morning) / 21358987 (evening) were missing from
  the ledger; both COMPLETED (0:0) and now regrade 17/17.
  All eight contain 200 frames and record 809.492 mm piece fall.
  Every run targets tree0_SPUR_component_8235 using RTX optical-Z depth.
  Tree1 is present, not targeted. No CLAHE task-completion advantage established.
  Post-release tracking loss remains recorded; old failed captures unchanged.
  No pending dependencies. Original submission/queue evidence remains historical.

DATA AND NEW RESEARCH PHASE
  Actual assets: 100 Envy + 100 UFO PLY, 100 + 100 cylinder metadata JSON,
  and all 200 existing USDA outputs present; family IDs 00000 through 00099.
  Inspected Data/* layout: RGB directories for 9 Envy / 0 UFO trees; depth
  directories for 100 Envy / 0 UFO. Directory presence is not frame completeness.
  Top-level train_manifest.csv: 9,120 rows, 80 Envy IDs; splits.json: 80 train /
  20 validation. Envy 00042/00065 are validation in this split only.
  Actual checkpoint exposure is unresolved: no verified unseen-tree or cross-family
  claim. DA2/DINO models not loaded/evaluated by this documentation audit.
  Isaac source/morning/noon/evening presets already exist. Blender generator
  has overcast/low-light paths; explicit controlled presets remain future work.
  Learned-depth offline, shadow and control stages are not executed.
  Eight Envy/UFO x four-light clips are PLANNED, with null job/video fields;
  the eight original-tree0 recordings are not substitutes. No new clips generated.
  Training flow/metric-student feeds, PPO runner and executed CuRobo remain gaps.

STORAGE
  Home df: 21.61 GiB used / 25 GiB; 3.39 GiB available (above caution threshold).
  Home du: 27.10 GiB; counter discrepancy unresolved, not evidence of free quota.
  HPC user share: at least 1,451,756,738,048 B (1.452 TB / 1.320 TiB).
  Three Kit screenshot directories unreadable; at most 48.24 GB to 1.5 TB warning.
  Primary repo: 10.14 GiB, including artifacts 8.65 GiB.
  Companion Data: 133.50 GiB; checkpoints: 692.45 GiB; da2_weights: 1.25 GiB.
  This documentation/evidence update projects <1 MiB added; no render allocation.
  New rendering needs a fresh storage preflight, per-view inventory and estimate.
  No files deleted or moved. Heavy outputs/caches must stay off home.

VALIDATION
  Fresh capture validator and independent sequence grader pass on all eight runs.
  Capture validator samples first/middle/last RGB/depth; grades use all telemetry.
  Tracked pruning CPU suite: 514 passed, 9 USD-dependent skips, 1 deselected.
  Full working tree: 545 passed, 1 failed, 9 skipped, 1 deselected.
  Existing untracked studio orchard test fails tree1 translation > 0.1 (actual 0).
  Studio remains unvalidated; its files are preserved without changes.
  Ruff lint/format pass: 122 maintained source/test/tool/renderer files.

DOCUMENTATION AND NEXT ACTION
  docs/RESEARCH_AUDIT_2026-09-20.md: current findings, priorities and inspect commands.
  docs/evidence/repository_audit_2026-09-20.json: jobs, hashes, grades, storage.
  docs/evidence/tree_inventory_2026-09-20.json: 200 assets and scoped split audit.
  docs/evidence/eight_clip_manifest_2026-09-20.json: planned conditions only.
  SLURM_JOBS.md: completed job IDs, raw array mapping and historical snapshots.
  NOTICE.md: local asset/model provenance and unresolved redistribution scope.
  Next: checkpoint-specific provenance and leakage, storage preflight, then a
  minimal paired Envy/UFO lighting and frozen-depth pilot before shadow control.
  After that: broader targets/views and feature surface-membership measurements.
  On each implementation/submission/result change, reconcile status, ledger,
  research notes and machine evidence; update notices when asset scope changes.
  Keep implemented, submitted, running, completed and validated claims distinct.

GitHub: https://github.com/joses2017smjh/isaac-sim-pruning-workflow

New preflights: **21370018** Envy 00000 and **21370019** UFO 00000 submitted; two 256x144 source/evening frames each, no dependencies. The 2 m plane test confirmed Cycles optical-Z exactly at center and off axis; lighting determinism passed.

Execution update: 21370005 COMPLETED (600 frames; learned-control gates FAIL); 21370018/19 Blender preflights COMPLETED; 21370021 DINO interface preflight COMPLETED (afterok:21370005). CPU suite 518 passed, 10 skips. New jobs: 21370026 short live DA2 shadow + family USD imports, 21370027/28 paired Envy/UFO pilots (24 frames each), all submitted without dependencies. Source SUN/Filmic baseline and fixed 8mm minimum spur-segment rule documented in frozen code; initial failures retained.

Preflight/result update: 21370039 COMPLETED (48 DA2 +48 DINO predictions); 21370026 FAILED before rendering (unsupported profile), 21370040 FAILED before rendering (mesh-only validator on cylinder assets), both retained. Corrected 21370047 RUNNING; both Envy/UFO USD imports now pass in Isaac. H.264 video smoke passed and was visually inspected. Initial one-tree-per-family evening degradation is diagnostic only, pending four-tree matrix.
