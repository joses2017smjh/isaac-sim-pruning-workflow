isaac-sim-pruning-workflow — status at 2026-09-05 10:03 PDT
Branch: develop
Owner / Git author: Jose Sanchez
GitHub: https://github.com/joses2017smjh/isaac-sim-pruning-workflow
Author email: 119717035+joses2017smjh@users.noreply.github.com

RUNNABLE DEMO

  python tools/run_pruning_demo.py --output-dir demo-output

The README contains a verified four-command CPU install. The command generates
an 18-second GIF, PNG poster, strict JSON measurements, and offline HTML sensor
replay. The three seed-7 scenarios use actual finite-cylinder ray casts,
configured sensor noise, the scripted ToF servo, and geometric cut/stop checks.

  Clear approach:  geometry accepted, 42 frames, 0.645 degree angle error.
  Sensor blackout: stopped, 20 frames; final four frames have zero returns
                   and identical tool poses.
  Nearby wood:     rejected, 35 frames, failure-volume intersection.

This is ideal tool motion, not arm dynamics or physical cutting. The final
insertion is a bounded open-loop stroke using known geometry. Metric estimates
in the fusion comparison are synthetic. No policy success rate is claimed.
Capture and explanation: docs/DEMO.md.

LOCAL VERIFICATION

  Fresh Python 3.10 venv: dependencies installed from package extras.
  PyTorch CPU 2.14.0, NumPy 2.2.6, Pillow 12.3.0, Matplotlib 3.10.9.
  133 tests passed, 1 simulator-marked test deselected.
  Ruff 0.14.10 lint and format checks pass across pruning source/tests/tools.
  CI installs dev+demo extras, tests, generates the demo, uploads artifacts.

FUNCTIONAL FIXES

  Scripted lateral feedback now moves toward target error.
  Roll correction rotates about approach Z and resolves PCA axis sign.
  Invalid depth samples cannot poison finite inverse-variance fusion.
  Isaac scene names resolve only after PhysX articulation initialization.
  Contact reporting is activated; full arm coverage is still unverified.
  IK uses the link-origin Jacobian with an explicit Torch view.
  Lab 3 xyzw and pruning-core wxyz poses convert at explicit boundaries.
  Failure reports preserve measured hold drift, sensor state, and joint traces.

CLUSTER EVIDENCE

  Gate 0: 21077170 passed (cube 1.5000 m, plane 2.0000 m, finite 100%).
  Fresh robot import: 21136450 passed (six UR joints, reviewed transforms).
  21153625 failed: raw Warp Jacobian had no clone(). This call is replaced.
  21185961 failed: after live stepping, tool hold exceeded 5 mm drift.
  21186027 failed: measured 20.12 mm drift, rotation 0.00309 rad.
           Both ToF grids: frame 2, 64/64 finite ranges.
           Contact shape (1,1,3): finite but full-arm coverage unverified.
           Controlled 5 mm motion was not reached.

No workflow job is queued. Baseline and PPO jobs remain unsubmitted.
All user jobs and exact accounting: SLURM_JOBS.md.

REMAINING RESEARCH WORK

  Diagnose live tool drift and verify all arm contact bodies.
  Replace Isaac's fixed smoke target with live oracle/curriculum targets and
  nearby-wood tensors. The CPU demo already integrates procedural geometry.
  Implement live flow, learned metric-depth inference, and episode-start DA2.
  Identify/calibrate the physical wrist camera; RGB remains disabled.
  Execute CuRobo planning; current baseline runner only reports readiness.
  Implement PPO runner, then run baseline-gated A-D x five seeds.
  Evaluate held-out Envy/UFO rollouts, sim2sim, and hardware transfer.

The optional bark texture and generated USD assets are not in the clone.
Root licensing remains unresolved for inherited code; see NOTICE.md.
Detailed gates: docs/ROADMAP.md. Reviewer audit: docs/REVIEWER_NOTES.md.
